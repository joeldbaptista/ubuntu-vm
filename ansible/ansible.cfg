[defaults]
inventory = inventory.ini
stdout_callback = yaml
retry_files_enabled = False

# The sandbox is destroyed and recreated often, and the network hands out the
# same addresses again, so a recorded host key would be wrong most of the time
# and would block the next run.
host_key_checking = False

[ssh_connection]
pipelining = True
